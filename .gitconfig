[user]
    name = Callum Oakley
    email = c.oakley108@gmail.com

[push]
    default = current

[alias]
    graph = log --graph --decorate --oneline -10
    root = rev-parse --show-toplevel
    ignore = "!f() { curl -L -s https://www.gitignore.io/api/$@; }; f"
    hash = rev-parse HEAD
    lcm = "!f() { echo $(git log -1 | tail -1); }; f"

[core]
    pager = less -x1,5
